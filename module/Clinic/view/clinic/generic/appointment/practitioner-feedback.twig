{% set title = 'Practitioner Feedback' %}
{% do headTitle(title) %}

<div class="page-header">
    <h1>
        {{ title }}
    </h1>
</div>
{% do form.prepare() %}

{{ form().openTag(form)|raw }}
    <div class='form-group'>
        {{ formRow(form.get('practitionerFeedback')) }}
    </div>
    <div class='form-group'>
        {{ formRow(form.get('submit')) }}
    </div>
{{ form().closeTag()|raw }}